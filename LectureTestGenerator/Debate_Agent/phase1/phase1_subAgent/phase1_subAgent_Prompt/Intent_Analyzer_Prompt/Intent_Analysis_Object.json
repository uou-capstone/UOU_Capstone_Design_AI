{
    "type": "object",
    "properties": {
        "intent": {
            "type": "string",
            "enum": [
                "APPROVE",
                "MODIFY",
                "UNCLEAR"
            ],
            "description": "사용자의 의도. 승인(APPROVE), 수정 요청(MODIFY), 혹은 불명확(UNCLEAR)."
        },
        "Modifications": {
            "type": "string",
            "description": "intent가 'MODIFY'일 경우에만 필수, 사용자가 요구한 구체적인 수정 내용을 담은 문자열"
        }
    },
    "required": [
        "intent"
    ]
}